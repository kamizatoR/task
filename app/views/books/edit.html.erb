<h1>Editing Book</h1>

<%= form_with model:@book do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :opinion %>
  <%= f.text_area :opinion %>

  <%= f.submit 'Create Book' %>
<% end %>
